rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Require authentication for any Firestore access.
      allow read, write: if request.auth != null;
    }
  }
}
